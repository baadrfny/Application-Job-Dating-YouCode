<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %}Job Dating{% endblock %}</title>

    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600;700&display=swap');

        :root {
            --ink: #0f1b2d;
            --muted: #5b677a;
            --primary: #4f3cf7;
            --primary-dark: #3d2cc4;
            --surface: #ffffff;
            --bg-top: #eef2ff;
            --bg-bottom: #e7eefb;
            --shadow: 0 18px 45px rgba(12, 22, 42, 0.16);
            --radius: 18px;
        }

        body {
            margin: 0;
            font-family: "Space Grotesk", "Segoe UI", Arial, sans-serif;
            color: var(--ink);
            background: radial-gradient(1200px 800px at 10% 10%, #f7f9ff 0, var(--bg-top) 40%, var(--bg-bottom) 100%);
            min-height: 100vh;
        }
    </style>
</head>
<body>

{% block content %}{% endblock %}

</body>
</html>
